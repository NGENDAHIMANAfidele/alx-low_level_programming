#include "main.h"
#include <stdio.h>
/**
 * main - ent the name
 * @argc: number of argument
 * @argv: array of argument
 * Return: 0;
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
